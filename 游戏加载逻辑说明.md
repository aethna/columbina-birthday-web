# 小游戏加载逻辑说明

> 本文档只根据当前源码分析加载流程，不涉及代码修改。
>
> 分析对象：`columbina-birthday-web-main/columbina-game/src/`

## 1. 总体结论

当前小游戏采用“两层加载”策略：

1. **进入大厅后立即显示界面**，不等待所有游戏资源下载完成。
2. **大厅挂载后按顺序启动后台预加载**，提前准备各个游戏的核心资源。
3. **用户点击某个游戏时，再以前台加载状态确认对应资源组已经完成**。
4. **只有对应资源组加载成功后，才切换到具体游戏页面**。
5. 图片资源不仅下载，还会通过 `Image.decode()` 或 `onload` 完成浏览器解码；非图片资源主要完成 `fetch` 和缓存准备。
6. BGM、角色语音、音效以及部分游戏运行时资源仍然存在独立的懒加载逻辑，不完全受资源组进度条控制。

核心入口和职责如下：

| 文件 | 职责 |
| --- | --- |
| `columbina-game/src/main.js` | 创建 Vue 应用、挂载 `App`、初始化本地化 |
| `columbina-game/src/App.vue` | 页面状态、资源分组、后台预加载、前台加载遮罩、加载失败重试 |
| `columbina-game/src/games/resourceLoader.js` | 资源下载、图片解码、去重、并发槽位和进度回调 |
| `columbina-game/src/games/bgm.js` | BGM 的首次下载、解码、缓存、切换和停止 |
| `columbina-game/src/games/voice.js` | 角色语音的首次下载、解码、缓存和播放 |
| `columbina-game/src/components/RunnerGame.vue` | 跑酷游戏挂载后的碰撞遮罩加载和关卡资源预取 |
| `columbina-game/src/components/BoardGame.vue` | 棋类页面挂载后的棋局初始化和 AI Worker 按需创建 |

## 2. 应用启动阶段

### 2.1 模块加载与静态资源 URL

`App.vue` 顶部通过 Vite `import` 引入图片、音频、视频和 Worker，例如：

- 飞行角色：`columbina-flight-tap.png`、`columbina-flight-default.png`
- 跑酷角色：`columbina-idle.png`、`columbina-jump.png`、`columbina-run-transparent.webp`
- 背景：大厅主视觉、四张关卡图
- 音频：大厅音乐、四段关卡音乐
- 棋类 Worker：井字棋和五子棋 Worker URL

这些 `import` 在构建后会变成带哈希的静态资源 URL，但**导入模块本身不等于所有资源已经下载并解码完成**。资源是否真正被浏览器请求，仍取决于后续的 `fetch`、`img`、`video`、Web Audio 或 Worker 使用行为。

`main.js` 只执行以下启动动作：

1. 导入全局样式和 `App`。
2. `createApp(App).mount('#app')` 挂载应用。
3. 调用 `initLocalization()` 初始化语言处理。

### 2.2 初始页面状态

`App.vue` 中的初始状态是：

- `screen = 'lobby'`：先显示游戏大厅。
- `assetLoading.active = false`：初始不显示资源加载遮罩。
- 飞行游戏和棋盘游戏的状态对象在内存中初始化。
- 最佳分数、棋类战绩从 `localStorage` 读取。

组件挂载后，`watch([screen, score 分段], applyBgm, { immediate: true })` 会立即执行，将大厅 BGM 交给 `setBgm(lobbyMusic)`。BGM 的实际下载和解码由 Web Audio 逻辑异步完成，不阻塞大厅显示。

## 3. 资源分组

`App.vue` 通过 `assetGroups` 把资源拆成若干组。资源组成功后会加入 `loadedAssetGroups`，之后再次进入同一游戏时不会重复等待。

| 资源组 | 资源内容 | 主要使用页面 |
| --- | --- | --- |
| `flightCore` | 飞行角色待机图、飞行角色跳跃图、月光障碍图、第一张关卡背景、第一段关卡音乐 | 云隙轻歌 |
| `runnerCore` | 跑酷角色待机图、跳跃图、跑步图、月光障碍图、第一张关卡背景、第一段关卡音乐 | 无尽巡游 |
| `tictactoeCore` | 玩家棋子、哥伦比娅棋子、井字棋棋盘、棋盘背景、品牌图标、第三段音乐、井字棋 Worker URL | 月亮棋 |
| `gomokuCore` | 玩家棋子、哥伦比娅棋子、五子棋棋盘、棋盘背景、品牌图标、第三段音乐、五子棋 Worker URL | 星月五子棋 |
| `stageTwo` | 第二张关卡背景、第二段关卡音乐 | 后续关卡 |
| `stageThree` | 第三张关卡背景、第三段关卡音乐 | 后续关卡 |
| `stageFour` | 第四张关卡背景、第四段关卡音乐 | 后续关卡 |

资源组并未覆盖所有运行时会出现的资源。例如：

- 大厅主视觉图片、主视觉视频和大厅 Logo 由大厅模板中的 `img`、`video` 或样式引用触发浏览器加载。
- BGM 的二进制下载和音频解码由 `bgm.js` 单独完成。
- 角色语音由 `voice.js` 在第一次播放某一类语音时懒加载。
- `sound.js` 的 UI、跳跃、得分等音效是 Web Audio 合成音，不需要下载音频文件。
- 跑酷的透明碰撞遮罩是在 `RunnerGame.vue` 挂载后从图片读取像素并生成的。

## 4. 大厅挂载后的后台预加载

### 4.1 启动时机和顺序

`App.vue` 的 `onMounted()` 调用 `beginBackgroundPreload()`。

后台预加载顺序固定为：

```text
flightCore
runnerCore
tictactoeCore
gomokuCore
stageTwo
stageThree
stageFour
```

每个资源组使用一个定时器启动：

- 第一个组在挂载后约 300ms 启动。
- 后续每个组间隔约 650ms 依次启动。

这些任务不会被 `beginBackgroundPreload()` 串行 `await`，而是分别在定时器中调用 `startAssetGroup()`。真正的同时下载数量由 `resourceLoader.js` 的全局下载槽位限制。

### 4.2 组级并发与全局并发

`startAssetGroup()` 调用 `loadAssetGroup()` 时显式设置 `concurrency: 1`，因此**同一个资源组内部一次只处理一个 URL**。

`resourceLoader.js` 另外维护一个全局下载槽位：

- `MAX_ACTIVE_DOWNLOADS = 2`。
- 所有资源组共用这两个槽位。
- 资源组内部的任务需要先通过 `acquireDownloadSlot()` 排队。
- 下载完成或失败后释放槽位，再唤醒队列中的下一个任务。

因此当前实际表现是：每个资源组内部顺序准备，但多个资源组之间最多允许两个资源下载任务同时进行。

后台预加载失败时，错误只在开发环境通过 `console.warn('[AssetPreload:...]', error)` 输出，不会显示前台错误遮罩，也不会阻止用户继续停留在大厅或点击其他游戏。

## 5. 单个资源的准备流程

资源组最终都会进入 `loadAssetGroup()`，其流程如下：

1. 对传入 URL 去重，得到当前资源组队列。
2. 立即回调 `onProgress(0, total)`。
3. 启动一个或多个 worker；本项目调用方传入 `concurrency: 1`，所以实际只有一个 worker。
4. worker 从队列取出 URL。
5. 如果传入的 `AbortSignal` 已取消，则抛出 `AbortError`。
6. 调用 `downloadAndPrepare(url, signal)`。
7. 单个资源完成后递增 `completed`，回调 `onProgress(completed, total)`。
8. 所有 worker 完成后，资源组 Promise resolve。

### 5.1 单资源去重

`resourceLoader.js` 有两层去重：

- `preparedAssets`：资源已经准备完成，后续直接返回。
- `inFlightAssets`：资源正在准备，后续请求直接复用当前 Promise。

这使得不同资源组同时需要同一张图片、同一段音乐或同一个 Worker URL 时，不会为同一个 URL 重复创建下载任务。

### 5.2 图片资源

对于扩展名为 `avif/gif/jpg/jpeg/png/webp` 的 URL：

1. 使用 `fetch(url, { cache: 'force-cache', signal })` 获取响应。
2. 检查 HTTP 状态；非 2xx 会抛出“资源下载失败”。
3. 转成 `Blob`。
4. 创建临时 `objectURL`。
5. 创建 `Image` 并设置 `src`。
6. 优先等待 `image.decode()`；不支持时等待 `image.onload`。
7. 释放临时 `objectURL`。
8. 将原始 URL 加入 `preparedAssets`。

所以资源组的图片进度表示的不只是网络响应完成，还包含了浏览器对图片的解码准备。

### 5.3 非图片资源

对音频文件和 Worker URL 等非图片资源，代码会完成 `fetch` 和 `blob()`，然后直接将 URL 标记为已准备；不会在 `resourceLoader.js` 中解码音频，也不会在这里创建 Worker。

因此资源组进度完成并不表示：

- 音频已经完成 Web Audio 解码；或
- Worker 已经真正启动并执行过一次任务。

## 6. 用户点击游戏时的前台加载

### 6.1 入口函数

大厅中的四个入口分别调用：

- `openGame()` → `prepareAssetGroup('flightCore', '云隙轻歌', ...)`
- `openRunner()` → `prepareAssetGroup('runnerCore', '无尽巡游', ...)`
- `openBoard('tictactoe')` → `prepareAssetGroup('tictactoeCore', '月亮棋', ...)`
- `openBoard('gomoku')` → `prepareAssetGroup('gomokuCore', '星月五子棋', ...)`

点击时先播放 UI 合成音，然后进入资源组准备流程。

### 6.2 已完成或正在进行中的资源组

`prepareAssetGroup()` 有两种快速路径：

1. 如果资源组已在 `loadedAssetGroups` 中，直接执行 `onReady()`，不显示加载遮罩。
2. 如果资源组已经由后台预加载启动，`startAssetGroup()` 会复用 `assetGroupTasks` 中的 Promise，不会重复创建资源组任务。

这意味着用户点击时可能发生以下情况：

- 资源组已经完成：立即进入游戏。
- 资源组正在后台下载：前台接管显示该组进度，等待后台任务结束。
- 资源组尚未开始：立即创建前台任务并显示进度。

### 6.3 前台状态和进度

点击后，`prepareAssetGroup()` 会：

1. 递增 `launchToken`，使当前点击成为最新一次启动请求。
2. 保存 `pendingLaunch = { group, title, onReady }`，供重试使用。
3. 设置 `foregroundGroup`，表示当前哪个资源组的进度要显示在 UI 上。
4. 从 `assetGroupProgress` 读取已有进度；如果没有则从 `0 / 资源总数` 开始。
5. 将 `assetLoading.active` 设置为 `true`。

模板中的资源加载遮罩显示：

- `PREPARING DREAM`
- 当前游戏名称
- 进度条
- `已完成数 / 总数`

百分比由 `loaded / total * 100` 四舍五入得到。

资源加载回调每次触发时都会更新对应资源组的进度；只有当 `foregroundGroup === group` 时，才会同步刷新前台遮罩。因此后台任务会继续运行，但不会在没有用户等待时强行显示进度。

### 6.4 加载完成后的页面切换

当前资源组 Promise resolve 后，`prepareAssetGroup()` 会检查 `launchToken`：

- Token 仍然是当前值：清除前台加载状态，执行 `onReady()`。
- Token 已变化：放弃本次页面切换，避免旧请求覆盖新的用户操作。

具体切换行为：

- 云隙轻歌：设置 `screen = 'game'`，下一帧滚动到页面顶部并重置飞行状态。
- 无尽巡游：停止旧动画，设置 `screen = 'runner'`，下一帧滚动到页面顶部。
- 棋类：设置棋类类型和 `screen = 'board'`，下一帧滚动到页面顶部。

## 7. 超时、失败、取消与重试

### 7.1 超时

每次 `startAssetGroup()` 会创建一个 `AbortController`，并设置 120 秒定时器。超时后调用 `controller.abort('timeout')`。

取消会影响：

- 等待下载槽位的任务；
- 使用该信号执行的 `fetch`；
- `loadAssetGroup()` 的 worker。

前台会将 `AbortError` 映射成：

> 下载时间过长，请检查网络后重试。

### 7.2 普通失败

HTTP 非成功状态、图片解码失败或其他异常会被映射成：

> 部分资源下载失败，请重试。

开发环境还会输出 `[AssetLoader:资源组]` 相关错误日志。

### 7.3 返回大厅

加载遮罩上的“返回大厅”按钮执行 `cancelAssetLoading()`，其行为是：

1. 递增 `launchToken`，让当前前台启动请求失效。
2. 停止正在播放的角色语音。
3. 清除 `foregroundGroup` 和 `pendingLaunch`。
4. 隐藏加载遮罩。

该函数本身**不会主动 abort 当前资源组 Promise**。因此已经开始的下载可能继续完成，并在完成后把资源组加入 `loadedAssetGroups`；只是不会再自动切换到游戏页面。

### 7.4 重新下载

发生错误时显示“重新下载”按钮。`retryAssetLoading()` 会取出 `pendingLaunch`，再次调用 `prepareAssetGroup()`。

由于已成功准备的 URL 保留在 `preparedAssets` 中，重试时这些资源会立即完成；失败或尚未准备的资源会继续走下载流程。资源组进度对象也会保留在 `assetGroupProgress` 中，用于显示当前已知进度。

## 8. 四个游戏进入后的后续加载

资源组加载成功只是“进入页面前的核心资源门槛”，进入组件后仍有各自的运行时加载行为。

### 8.1 云隙轻歌

进入 `screen = 'game'` 后：

- `playerImage` 根据 `isJumping` 在 `flightIdle` 和 `flightJump` 之间切换。
- 第一张背景和第一段音乐已在 `flightCore` 中准备。
- 当前背景由 `score` 每 10 分切换一次，使用四张关卡背景循环选择。
- 得分达到以 9 结尾的分数时，通过 `new Image()` 预取下一张背景，尽量减少到达下一阶段时的显示延迟。
- BGM 由 `applyBgm()` 根据页面和分数阶段切换；真正的音频 fetch/decode 由 `bgm.js` 懒执行。
- 飞行游戏的碰撞是几何矩形判定，不需要额外的透明像素碰撞遮罩。

### 8.2 无尽巡游

`RunnerGame.vue` 挂载后会立即：

1. 调用 `loadAlphaMask(moonObstacle)`。
2. 将障碍物图片绘制到 Canvas。
3. 读取像素 Alpha 通道，生成透明遮罩。
4. 后续碰撞检测只把不透明像素视为障碍物。

这个遮罩生成过程没有被 `App.vue` 的资源进度条单独展示，也没有作为进入跑酷页面前的第二个等待条件；组件先挂载，遮罩异步生成。

跑酷过程中：

- 背景在每 10 分切换阶段。
- 得分达到阶段切换前的临界值时，通过 `new Image()` 预取下一张背景。
- BGM 随阶段切换。
- 跑步图加载失败时，模板回退到待机图；跳跃图失败时最终显示文字占位符。

### 8.3 月亮棋与星月五子棋

棋类页面的核心图片、棋子、背景、品牌图标和对应 Worker URL 已包含在各自资源组中。资源组完成后才挂载 `BoardGame.vue`。

组件挂载后的流程是：

1. 初始显示设置弹层，而不是直接开始对局。
2. 用户选择难度和先手方。
3. 点击“开始对局”后确认设置并重置棋局。
4. 如果哥伦比娅先手，延迟约 120ms 调用 AI。
5. 井字棋在需要计算时创建模块 Worker；如果浏览器不支持 Worker，则回退到主线程 AI。
6. 五子棋通过 `GomokuAI.getBestMove()` 异步计算，并使用 AbortController 取消过期请求。

资源组中的 Worker URL 预加载只负责把 URL 对应的资源加入缓存准备流程；真正的 Worker 实例是在 AI 首次计算时创建。

棋类 BGM 使用第三段关卡音乐。角色语音和音效仍按首次触发时加载或合成。

## 9. 音频加载逻辑

### 9.1 BGM

`bgm.js` 使用 Web Audio API：

1. 第一次调用 `setBgm(source)` 时创建 `AudioContext`。
2. 通过 `fetch(source)` 获取音频数据。
3. 转成 `ArrayBuffer`。
4. 调用 `decodeAudioData()` 解码。
5. 将解码 Promise 缓存到 `buffers` Map。
6. 创建循环播放的 `AudioBufferSourceNode` 和 GainNode。
7. 新旧 BGM 交叉淡入淡出。

同一个 source 的后续播放复用已缓存的解码 Promise。BGM 加载异常会被静默捕获，不会显示资源加载失败遮罩。

页面隐藏、离开或进入 bfcache 时，App 会停止或挂起 BGM；返回页面时恢复 AudioContext 并根据当前页面重新应用 BGM。

### 9.2 角色语音

`voice.js` 使用 `import.meta.glob()` 在构建时收集所有语音文件 URL，但不会因此立即下载所有语音。

第一次播放某类语音时：

1. 根据事件类型从语音池中选择一个 URL。
2. `fetch()` 音频文件并转成 `ArrayBuffer`。
3. 使用 `decodeAudioData()` 解码。
4. 将解码 Promise 缓存。
5. 创建一次性的 BufferSource 播放。

语音有概率和冷却时间控制。离开游戏、返回大厅或组件卸载时，未完成或正在播放的语音请求会通过 request id 失效控制和节点停止逻辑被取消/忽略。

### 9.3 合成音效

`sound.js` 不读取音频文件。UI、开始、跳跃、得分、碰撞和棋子等音效都通过振荡器和 GainNode 实时合成，因此不进入资源组下载进度。

## 10. 生命周期清理

`App.vue` 卸载时会：

- 递增 `launchToken`，使未完成的页面切换失效。
- 停止语音。
- 清理后台预加载定时器。
- abort 所有资源组任务。
- 取消游戏动画帧。
- 清理角色动作定时器和事件监听。
- 停止 BGM。

各子游戏组件也会在卸载时取消自己的动画帧、事件监听、语音和 AI 请求。

## 11. 当前加载链路摘要

```text
打开游戏页面
  ↓
App 挂载，显示 lobby
  ↓
立即尝试播放大厅 BGM（独立懒加载）
  ↓
300ms 后启动 flightCore 后台预加载
  ↓
按 650ms 间隔启动后续资源组
  ↓
用户点击某个游戏
  ↓
检查资源组是否已完成 / 是否已有进行中的 Promise
  ↓
未完成 → 显示前台进度遮罩，继续等待
  ↓
资源下载 + 图片解码完成
  ↓
清除遮罩，切换 screen，挂载对应游戏组件
  ↓
组件执行自己的运行时加载：碰撞遮罩、BGM 解码、语音、AI Worker 等
```

整体上，当前逻辑把“进入游戏前必须准备好的核心静态资源”和“进入游戏后按实际需要产生的运行时资源”分开处理；前者有统一的资源组进度和错误提示，后者由具体组件或音频模块独立管理。
